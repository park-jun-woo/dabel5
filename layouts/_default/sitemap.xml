{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  {{- if or (and .IsPage (or (not .Date.IsZero) (not .Lastmod.IsZero))) (and .IsSection (gt (len .Pages) 0)) .IsHome }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if .IsTranslated }}
    {{- range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Lang }}" href="{{ .Permalink }}" />
    {{- end }}
    <xhtml:link rel="alternate" hreflang="{{ .Lang }}" href="{{ .Permalink }}" />
    {{- range .AllTranslations }}
    {{- if eq .Lang "en" }}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ .Permalink }}" />
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    <priority>{{ if .IsHome }}1.0{{ else if .IsPage }}0.9{{ else }}0.5{{ end }}</priority>
  </url>
  {{- end }}
  {{- end }}
</urlset>
